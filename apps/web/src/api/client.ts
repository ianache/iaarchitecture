import type { AnalysisSummary } from "@architecture-ai/domain";

export interface ApiClient { createAnalysis(requirements: string, knowledgeRevision: string): Promise<{ id: string; status: unknown }>; listAnalyses(): Promise<{ analyses: AnalysisSummary[] }>; generatePackage(id: string, outputDirectory?: string): Promise<any>; getPackage(id: string): Promise<any>; getTraceability(id: string): Promise<any>; getDecisions(id: string): Promise<any>; reviewDecision(id: string, action: "review" | "approve" | "reject" | "request-changes"): Promise<unknown>; getAudit(id: string): Promise<any>; }
export function createApiClient(baseUrl: string): ApiClient { const json = async (path: string, init?: RequestInit) => { const response = await fetch(`${baseUrl}${path}`, { headers: { "content-type": "application/json" }, ...init }); if (!response.ok) throw new Error(`API request failed: ${response.status}`); return response.json(); }; return { createAnalysis: (requirements, knowledgeRevision) => json("/analyses", { method: "POST", body: JSON.stringify({ requirements, knowledgeRevision }) }), listAnalyses: () => json("/analyses"), generatePackage: (id, outputDirectory) => json(`/packages/${id}/generate`, { method: "POST", body: JSON.stringify({ outputDirectory }) }), getPackage: (id) => json(`/packages/${id}`), getTraceability: (id) => json(`/packages/${id}/traceability`), getDecisions: (id) => json(`/packages/${id}/decisions`), reviewDecision: (id, action) => json(`/decisions/${id}/${action}`, { method: "POST", body: JSON.stringify({ reviewer: "web-user" }) }), getAudit: (id) => json(`/decisions/${id}/audit`) }; }
