import { Command } from "commander";
export function createCli(): Command { const program = new Command(); program.name("architecture-ai").description("Architecture AI automation interface"); program.command("analyze").requiredOption("--requirements <text>").requiredOption("--revision <git-revision>").action((options) => console.log(JSON.stringify({ command: "analyze", ...options }))); program.command("package").argument("<analysisId>").action((analysisId) => console.log(JSON.stringify({ command: "package", analysisId }))); program.command("review").argument("<decisionId>").requiredOption("--action <action>").action((decisionId, options) => console.log(JSON.stringify({ command: "review", decisionId, ...options }))); return program; }
if (import.meta.url === `file://${process.argv[1]}`) createCli().parse();
