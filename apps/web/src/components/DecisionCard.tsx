import type { ArchitectureDecision } from "@architecture-ai/domain";
export function DecisionCard({ decision, onAction }: { decision: ArchitectureDecision; onAction: (action: "approve" | "reject" | "request-changes") => void }) { return <article><h3>{decision.id}: {decision.title}</h3><p>{decision.decision}</p><p><strong>Status:</strong> {decision.status}</p><p><strong>Evidence:</strong> {decision.evidenceIds.join(", ") || "Insufficient corporate evidence"}</p><button onClick={() => onAction("approve")}>Approve</button><button onClick={() => onAction("reject")}>Reject</button><button onClick={() => onAction("request-changes")}>Request changes</button></article>; }
